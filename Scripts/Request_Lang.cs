using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using YG;

public class Request_Lang : MonoBehaviour
{
    /*
    public List<GameObject> ALL_TXT;
    private bool translated = false;
    private void Update()
    {
        Translate();
    }
    private void Search()
    {
        foreach (GameObject TXT in ALL_TXT)
        {
            TXT.GetComponent<Text_localisation>().Reload_language();
        }
        translated = true;
    }
    private void Translate()
    {
        if (!All_Upgrades.ManualChangedLang && !translated && YandexGame.SDKEnabled)
        {
            switch (YandexGame.EnvironmentData.language)
            {
                case "ru":
                    if (Current_language.LANGUAGE != "RUSSIAN") { Current_language.LANGUAGE = "RUSSIAN"; Search(); }
                    break;
                case "en":
                    if (Current_language.LANGUAGE != "ENGLISH") { Current_language.LANGUAGE = "ENGLISH"; Search(); }
                    break;
                case "kk":
                    if (Current_language.LANGUAGE != "KAZAKH") { Current_language.LANGUAGE = "KAZAKH"; Search(); }
                    break;
                default:
                    if (Current_language.LANGUAGE != "ENGLISH") { Current_language.LANGUAGE = "ENGLISH"; Search(); }
                    break;
            }
        }
    }*/
}
